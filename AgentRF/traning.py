from utils.models import PolicyNetworkModel, DQNAgent

# Agent = PolicyNetworkModel(input_dim=None, hidden_dim=64, output_dim=None, dropout=0.5)

# Agent.traningM()

Agent = DQNAgent()

Agent.training_loop()